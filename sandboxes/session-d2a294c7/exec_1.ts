require('./globals.js');
const files = require('../../tools/files/index.ts');
const search = require('../../tools/search/index.ts');
const message = require('../../tools/message/index.ts');
const agents = require('../../tools/agents/index.ts');
const srcAgent = require('../../tools/srcAgent/index.ts');
const heartbeat = require('../../tools/heartbeat/index.ts');
const homework = require('../../tools/homework/index.ts');

// Agent code execution
(async () => {
// Package requires


// Вызываю подагента-эксперта для создания списка адаптаций
await agents.call("biologist_9grade", `Создай максимально длинный список реальных примеров адаптаций для 9 класса.

Требования:
1. Четыре типа адаптаций:
   - БИОЛОГИЧЕСКИЕ АДАПТАЦИИ (общие, на уровне вида/популяции)
   - ФИЗИОЛОГИЧЕСКИЕ АДАПТАЦИИ (функции органов и систем)
   - МОРФОЛОГИЧЕСКИЕ АДАПТАЦИИ (строение, форма органов)
   - ПОВЕДЕНЧЕСКИЕ АДАПТАЦИИ (инстинкты, сложное поведение)

2. Каждый пример: краткий (1-2 предложения), но информативный
3. Охвати максимальное разнообразие:
   - Млекопитающие, птицы, рыбы, земноводные, пресмыкающиеся
   - Насекомые, паукообразные, другие беспозвоночные
   - Растения (цветковые, хвойные, споровые)
   - Микроорганизмы (бактерии, простейшие)
   - Грибы, лишайники

4. Минимум 20-25 примеров в каждой категории — чем больше, тем лучше!
5. Формат: markdown, с заголовками и маркированными списками

Сохрани результат в файл adaptations_list.md в следующем формате:

# Адаптации организмов (9 класс)

## 1. Биологические адаптации (общие, на уровне вида/популяции)

### Растения
- [примеры растений]

### Животные
- [примеры животных]

### Микроорганизмы
- [примеры]

## 2. Физиологические адаптации (функции органов и систем)
[аналогичная структура]

## 3. Морфологические адаптации (строение, форма органов)
[аналогичная структура]

## 4. Поведенческие адаптации (инстинкты, сложное поведение)
[аналогичная структура]

Важно: примеры должны быть РЕАЛЬНЫМИ, из школьной программы или научных источников. Не выдумывай!`);

console.log("Задача подагенту отправлена");
})().catch(err => {
  console.error(err);
  process.exit(1);
}).then(() => {
  // Keep the event loop alive to allow promise chains (like .then() calls) to complete
  return new Promise(resolve => setTimeout(resolve, 500));
}).catch(err => {
  console.error('Error in promise chain:', err);
  process.exit(1);
});
